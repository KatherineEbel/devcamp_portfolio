<p class="text-center pt-3">
  <%= link_to "Add a new Project", new_portfolio_path if logged_in? :site_admin%>
</p>
<div class="album py-5 bg-light">
  <div class="container">
    <div class="row <%= 'sortable' if logged_in? :site_admin %>">
      <%= render @portfolio_items %>
    </div>
  </div>
</div>
